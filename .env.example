# Turso Database Configuration
# ============================

# Option 1: Remote-only database (no local replica)
# TURSO_DATABASE_URL=https://your-database.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# Option 2: Embedded replica (local file synced with cloud)
# This provides local reads with cloud backup and sync
TURSO_DATABASE_URL=file:modeler.db
TURSO_SYNC_URL=https://your-database.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# Optional: Auto-sync interval in seconds (0 = disabled, manual sync only)
# Example: 60 = sync every minute
TURSO_SYNC_INTERVAL=60

# Vector Search (requires OpenAI API key)
# =======================================
ENABLE_VECTOR_SEARCH=false
# OPENAI_API_KEY=your-openai-api-key
